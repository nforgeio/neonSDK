@namespace Neon.Tailwind

@inherits HtmlElement

    <HtmlElement 
    Id="@Id"
    aria-expanded="@((IsVisible ?? isVisible).ToString())"  
    @attributes="Attributes"
    @ref="rootElement" >
    @if (IsVisible ?? isVisible) {
    @ChildContent
    }

</HtmlElement>
